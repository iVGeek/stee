/**
 * No placeholder or bot reviews are ever seeded — the testimonials section
 * only ever shows real, admin-approved client feedback.
 */
export async function seedIfEmpty(): Promise<void> {
  // Intentionally empty.
}
